services:
  - type: web
    name: autopal-pi-system
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start:cloud
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_SSL
        value: "true"
      - key: DATABASE_URL
        sync: false
